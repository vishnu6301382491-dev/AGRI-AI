/**
 * Users Database
 * In production, this should be replaced with a real database (MongoDB, PostgreSQL, etc.)
 * For now, users are stored in memory during the session
 */

// Initialize with empty array - users register via sign-up form
const users = [];

module.exports = users;
